@implements ITabItem

<li class="tab-item @IsSelected()" @onclick="SetTab">
    @Title
</li>

@code {
    [CascadingParameter]
    public Tab Owner { get; set; }

    [Parameter]
    public RenderFragment Title { get; set; }
    [Parameter]
    public RenderFragment ChildContent { get; set; }

    private string IsSelected()
    {
        if(Owner.ActiveTab == this)
        {
            return "selected";
        }
        return "";
    }

    private void SetTab()
    {
        Owner.SetTab(this);
    }

    protected override void OnInitialized()
    {
        Owner.AddTab(this);
    }
}
