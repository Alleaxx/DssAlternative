<Window x:Class="DSSView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DSSView"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="900">
    <Window.Resources>
        <Style TargetType="{x:Type TreeViewItem}">

            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                   Color="Gainsboro"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                   Color="#FFCDCDCD"/>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Grid.ColumnSpan" Value="2" />
                </Style>
            </Style.Resources>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
            <Setter Property="FontWeight" Value="Normal" />

        </Style>
    </Window.Resources>
    <Window.DataContext>
        <local:View />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="Tabs" ItemsSource="{Binding Collection}" Background="WhiteSmoke" Margin="0,5,0,15" BorderThickness="0" SelectedItem="{Binding Selected}" Grid.ColumnSpan="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontSize="18" />
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <GroupBox Header="Начало" Grid.Row="1" DataContext="{Binding Selected}">
            <TreeView Grid.Column="0" BorderThickness="0" ItemsSource="{Binding Tabs}" SelectedItemChanged="TreeView_SelectedItemChanged" >
                <TreeView.Resources>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type local:Tab}">
                        <Border Background="{Binding Color.Background}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{Binding Color.Main}" FontSize="14" Margin="0,0"/>
                                <TextBlock Text="{Binding Tooltip}" Foreground="{Binding Color.Additional}" FontSize="10"  Margin="0,3"/>
                            </StackPanel>
                        </Border>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Reports}" DataType="{x:Type local:DataView}">
                        <Border Background="{Binding Color.Background}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{Binding Color.Main}" FontSize="14" Margin="0,0"/>
                                <TextBlock Text="{Binding Tooltip}" Foreground="{Binding Color.Additional}" FontSize="10"  Margin="0,3"/>
                            </StackPanel>
                        </Border>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Criterias}" DataType="{x:Type local:InfoCriteriasView}">
                        <Border Background="{Binding Color.Background}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{Binding Color.Main}" FontSize="12" Margin="0,0"/>
                                <TextBlock Text="{Binding Tooltip}" Foreground="{Binding Color.Additional}" FontSize="10"  Margin="0,3"/>
                            </StackPanel>
                        </Border>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type local:CriteriaView}">
                        <Border Background="{Binding Color.Background}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{Binding Color.Main}" FontSize="12" Margin="0,0"/>
                                <TextBlock Text="{Binding Tooltip}" Foreground="{Binding Color.Additional}" FontSize="10"  Margin="0,3"/>
                            </StackPanel>
                        </Border>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </GroupBox>
        <ContentPresenter Grid.Column="1" Grid.Row="1" Content="{Binding Selected.Selected.Object}">
            <ContentPresenter.Resources>
                <DataTemplate DataType="{x:Type local:TabMatrixCreator}">
                    <StackPanel>
                        <TextBlock Text="Имя" />
                        <TextBox Text="{Binding Info.Name}" />
                        <TextBlock Text="Строк" />
                        <TextBox Text="{Binding Info.Rows}" />
                        <TextBlock Text="Стобцов" />
                        <TextBox Text="{Binding Info.Cols}" />
                        <Button Command="{Binding AddMatrixCommand}" Content="Добавить" CommandParameter="{Binding Info}" />
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:DataView}">
                    <GroupBox Header="Матрица">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding RowsInfo}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding Cells}" Padding="10,0,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type local:CellValue}">
                                                    <Border Height="35" Width="100" BorderThickness="0" BorderBrush="Gainsboro">
                                                        <TextBox Text="{Binding Value}" IsEnabled="{Binding IsEditable}"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ListView ItemsSource="{Binding InfoCriterias.Criterias}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Критерий" DisplayMemberBinding="{Binding Name}" />
                                        <GridViewColumn Header="Результат" DisplayMemberBinding="{Binding Result}"  Width="70"/>
                                        <GridViewColumn Header="Выборы">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ItemsControl ItemsSource="{Binding Choices}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding}" Margin="10,0" />
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <WrapPanel />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                    </ItemsControl>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </GroupBox>
                </DataTemplate>
                
                <DataTemplate DataType="{x:Type local:Tab}">
                    <GroupBox Header="{Binding Name}">

                    </GroupBox>
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:InfoCriteriasView}">
                    <GroupBox Header="{Binding Name}">
                        <TextBlock Text="{Binding Criterias.Count}" />
                    </GroupBox>
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:CriteriaView}">
                    <GroupBox Header="{Binding Name}">
                        <TextBlock Text="{Binding Result}" />
                    </GroupBox>
                </DataTemplate>
            </ContentPresenter.Resources>
        </ContentPresenter>
    </Grid>
</Window>
